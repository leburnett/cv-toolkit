[pytest]
testpaths = tests
markers =
    slow: drives headless Chrome; seconds rather than milliseconds
# -ra prints a summary of anything skipped, so a silently skipped
# Chrome-dependent test can't be mistaken for a passing one.
addopts = -ra
